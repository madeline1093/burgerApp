<h1>Eat da Burger!</h1>

<p>Available Burgers</p>

<ul>
    {{#each burgers}}
        {{#unless isEaten}}
            {{> burgers/burger-block eaten=true}}
        {{/unless}}
    {{/each}}
</ul>

<p>Burgers you ate</p>

<ul>
    {{#each burgers}}
        {{#if isEaten}}
            {{> burgers/burger-block eaten=false}}
        {{/if}}
    {{/each}}
</ul>

<h3>What Burger do you want?</h3>
<form class="create-form">

  <div class="form-group">
    <label for="burgerName">New Burger:</label>
    <input type="text" id="burgerName" name="burger_name" >
  </div>

  <button type="submit" class='addBurger'>Add Burger</button>
</form>
