<h1>Eat da Burger!</h1>

<p>Non-eaten Burgers</p>

<ul>
    {{#each burgers}}
        {{#unless isEaten}}
            {{> burgers/burger-block eaten=true}}
        {{/unless}}
    {{/each}}
</ul>

<p>Burgers that are eaten!</p>

<ul>
    {{#each burgers}}
        {{#if isEaten}}
            {{> burgers/burger-block eaten=false}}
        {{/if}}
    {{/each}}
</ul>

<h3>What Burger do you want?</h3>
<form class="create-form">

  <div class="form-group">
    <label for="burgerName">Cat Name:</label>
    <input type="text" id="burgerName" name="name" >
  </div>

  <button type="submit" >Add Burger</button>
</form>
